using System;
using System.Collections.Generic;
using System.Text;

namespace Lab_4_Collections
{
    class Comparer : IComparer<OilProducts>
    {
        /*public int Compare(OilProducts x , OilProducts y)
        {
            if (x.volume > y.volume)
                return 1;
            else if (x.volume < y.volume)
                return -1;
            else
                return 0;
        }*/
      
    }
}
